{
  "name": "zig",
  "id": "zig",
  "version": "1.1.0",
  "description": "Installs the Zig programming language and ZLS (Zig Language Server)",
  "options": {
    "version": {
      "type": "string",
      "default": "master",
      "description": "Zig version to install, defaults to master. (example: 0.15.2)"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
